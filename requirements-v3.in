# v3.0 FastAPI async stack — direct dependencies
# Install with: pip install -r requirements-v3.txt

# ASGI framework & servers
fastapi>=0.115
uvicorn[standard]>=0.32
granian>=1.6

# Validation & settings
pydantic>=2.10
pydantic-settings>=2.6

# Async database
sqlalchemy>=2.0.36
aiosqlite>=0.20           # async SQLite (P2P node)
asyncpg>=0.30             # async PostgreSQL (relay)
alembic>=1.14

# HTTP client
httpx>=0.28

# Fast JSON
orjson>=3.10

# Async event loop
uvloop>=0.21; sys_platform == "linux"

# Async DNS (for Tor integration)
aiodns>=3.2

# Structured logging
structlog>=24.4

# Observability
opentelemetry-distro>=0.50b0
opentelemetry-exporter-otlp>=1.29
prometheus-client>=0.21

# Error monitoring
sentry-sdk[fastapi]>=2.0

# Session signing
itsdangerous>=2.2

# Socket.IO (ASGI mode — replaces flask-socketio in Phase 2c)
python-socketio>=5.11

# Cryptography (shared)
cryptography>=49.0

# Testing
pytest>=8.0
pytest-asyncio>=0.24
pytest-cov>=6.0
httpx>=0.28             # async test client for FastAPI
hypothesis>=6.0
